name: CI

on:
  push:
    branches: ["**"]
    tags: ["*"]
  pull_request:
    branches: ["**"]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  CONFIGURATION: Release
  PROJECT_PATH: QaaS.Runner.sln
  PACKAGE_OUTPUT_DIRECTORY: ${{ github.workspace }}\BuildOutput
  RESTORED_PACKAGES_PATH: ${{ github.workspace }}\RestoredPackages

jobs:
  build-test-deploy:
    name: Build, Test & Deploy
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Detect mirror tag
        id: mirror_tag
        shell: bash
        run: |
          if [[ "${GITHUB_REF}" =~ ^refs/tags/([0-9]+\.[0-9]+\.[0-9]+(-alpha\.[0-9]+)?)$ ]]; then
            echo "is_mirror_tag=true" >> "$GITHUB_OUTPUT"
            echo "tag=${BASH_REMATCH[1]}" >> "$GITHUB_OUTPUT"
          else
            echo "is_mirror_tag=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Restore packages
        run: dotnet restore ${{ env.PROJECT_PATH }} --packages ${{ env.RESTORED_PACKAGES_PATH }}

      - name: Build project
        run: dotnet build ${{ env.PROJECT_PATH }} --no-restore --configuration ${{ env.CONFIGURATION }}

      - name: Test solution
        run: dotnet test ${{ env.PROJECT_PATH }} --configuration ${{ env.CONFIGURATION }} --no-restore --no-build --maxcpucount

      - name: Get Version
        if: startsWith(github.ref, 'refs/tags/')
        id: version
        uses: battila7/get-version-action@v2

      - name: Validate SemVer tag
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: |
          VERSION="${{ steps.version.outputs.version-without-v }}"
          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-(alpha|beta)\.[0-9]+)?$ ]]; then
            echo "Error: '$VERSION' is not a valid version format."
            exit 1
          fi

      - name: Pack project
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          dotnet pack ${{ env.PROJECT_PATH }} `
            --no-restore --no-build `
            --configuration ${{ env.CONFIGURATION }} `
            --include-symbols `
            -p:PackageVersion=${{ steps.version.outputs.version-without-v }} `
            --output ${{ env.PACKAGE_OUTPUT_DIRECTORY }}

      - name: Push package
        if: startsWith(github.ref, 'refs/tags/')
        run: dotnet nuget push ${{ env.PACKAGE_OUTPUT_DIRECTORY }}\*.nupkg -k ${{ secrets.NUGET_AUTH_TOKEN }} -s https://api.nuget.org/v3/index.json --skip-duplicate

      - name: Write restored packages metadata
        if: steps.mirror_tag.outputs.is_mirror_tag == 'true'
        shell: pwsh
        run: |
          $metadata = @{
            repository = "${{ github.repository }}"
            tag = "${{ steps.mirror_tag.outputs.tag }}"
            runId = "${{ github.run_id }}"
            createdAtUtc = (Get-Date).ToUniversalTime().ToString("o")
          } | ConvertTo-Json

          $metadataPath = Join-Path "${{ env.RESTORED_PACKAGES_PATH }}" "restore-artifact-metadata.json"
          $metadata | Set-Content -Path $metadataPath

      - name: Upload restored packages artifact
        if: steps.mirror_tag.outputs.is_mirror_tag == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: restored-packages
          path: ${{ env.RESTORED_PACKAGES_PATH }}
          if-no-files-found: error
          retention-days: 7

