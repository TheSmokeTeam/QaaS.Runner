# QaaS.Runner.Storage

The QaaS project responsible for for managing the storage of SessionData in external sources.

Contains the `Storage` configuration section configuration object.

Contains `Store` and `Retrieve` functionality that works on the same configuration.
